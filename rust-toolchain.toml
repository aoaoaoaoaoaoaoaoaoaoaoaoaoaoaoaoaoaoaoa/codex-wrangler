# Non-versioned on purpose: use the active system rustup channel, not a stale
# Rust version copied from the starter.

[toolchain]
channel = "stable"
components = ["clippy", "rustfmt"]
profile = "minimal"
